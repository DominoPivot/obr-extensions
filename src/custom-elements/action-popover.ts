import "./action-popover.css";

/**
 * Container for an OBR extension's action popover.
 * It should contain a header element, whose first child holds the name of the extension.
 * The custom element provides no behavior at the moment.
 */
customElements.define("action-popover", class extends HTMLElement {});
