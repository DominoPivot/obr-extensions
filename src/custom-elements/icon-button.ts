import "./icon-button.css";

/**
 * A transparent button meant to hold an icon, which raises on hover.
 *
 * The custom element doesn't provide any behavior at the moment.
 * Use the icon-button class on a button element to support interaction.
 */
customElements.define("icon-button", class extends HTMLElement {});
